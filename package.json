{
  "name": "hamaru",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "10x10 block puzzle. Cloudflare Workers + autonomous kaizen loop.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/velfon/hamaru.git"
  },
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "npm run validate:config && npm run typecheck && npm run lint",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "sim": "echo TODO && exit 1",
    "validate:config": "tsx scripts/validate-config.ts",
    "i18n:check": "echo TODO && exit 1",
    "golden:update": "tsx scripts/golden-update.ts",
    "metrics:pull": "echo TODO && exit 1",
    "experiment:eval": "echo TODO && exit 1",
    "deploy": "echo TODO && exit 1"
  },
  "dependencies": {
    "zod": "4.6.5"
  },
  "devDependencies": {
    "@eslint/js": "9.39.5",
    "@types/node": "24.13.5",
    "@vitest/coverage-v8": "4.1.11",
    "eslint": "9.39.5",
    "globals": "17.12.0",
    "prettier": "3.9.7",
    "tsx": "4.23.13",
    "typescript": "6.0.3",
    "typescript-eslint": "8.70.0",
    "vite": "8.3.0",
    "vitest": "4.1.11"
  }
}
